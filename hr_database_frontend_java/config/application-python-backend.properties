# Python backend stack config
# Quarkus frontend (container) → Python backend (container, same Docker network)
quarkus.rest-client.hr-backend.url=http://hr_database:8081
quarkus.rest-client.hr-backend.scope=jakarta.inject.Singleton
