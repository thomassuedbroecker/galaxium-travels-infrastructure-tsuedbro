# VM-side client or application template for the split host+VM topology.
# Usage:
#   cp local-container/vm-client.env.template local-container/vm-client.env
#   source local-container/vm-client.env

KEYCLOAK_BASE_URL=http://$(ipconfig getifaddr en0):8086
KEYCLOAK_TOKEN_URL=http://$(ipconfig getifaddr en0):8086/realms/galaxium/protocol/openid-connect/token
MCP_SERVER_URL=http://$(ipconfig getifaddr en0):8084/mcp
